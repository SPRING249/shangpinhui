//1. patch--书写自己的业务逻辑，也可以处理异步
const actions={}
//2.commit--修改state的唯一手段
const mutations={}
// 3.mutate--state :存储数据
const state={
}
// 简化仓库数据
const getters={}

export default({
    namespaced:true,
    actions,mutations,state,getters
})